package model;

public class Cupcake extends Confectionary {
	public Cupcake(String type, String name, String softness, boolean hasTopping, String[] toppings, Payment payment) {
		super(type, name, softness, hasTopping, toppings, payment);
	}
}
